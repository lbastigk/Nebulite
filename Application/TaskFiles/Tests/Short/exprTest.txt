set-fps 60
wait 10

# retrieve from global
eval echo Hello world! $(global.time.t)

# try to retrieve from self in global level: should link to empty doc and yield 0 or ""
eval echo Hello world! $(self.posX)