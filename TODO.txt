optionsmenu no freeze
class for input not freeze

Full implementation of windows compiling in vscode, perhaps cross-compiling with mingw32 ?
-> Better integration of everything in tasks.json

Implement a shell script for installing all relevent environments, test with docker environment?

Offload classes into dlls?
Learn CMake

- identify sprite sheets, call same sheets with sdl_rendergeometry
- create texture of static background on level load to reduce rendertime (use roc[0] for bg?)

#######

Change rendering pipeline to work like this:

// Create a texture to use as a render target (e.g., size 800x600)
SDL_Texture* sceneTexture = SDL_CreateTexture(renderer, SDL_PIXELFORMAT_RGBA8888, SDL_TEXTUREACCESS_TARGET, 800, 600);

// Set the render target to the sceneTexture
SDL_SetRenderTarget(renderer, sceneTexture);

// Clear the render target (optional, depending on if you want transparency)
SDL_SetRenderDrawColor(renderer, 0, 0, 0, 255);
SDL_RenderClear(renderer);

// Draw your objects to the render target (sceneTexture)
SDL_RenderCopy(renderer, objectTexture1, NULL, &objectRect1); // Example object 1
SDL_RenderCopy(renderer, objectTexture2, NULL, &objectRect2); // Example object 2
// ... repeat for all objects ...

// Set the render target back to the screen
SDL_SetRenderTarget(renderer, NULL);

// Clear the screen
SDL_SetRenderDrawColor(renderer, 0, 0, 0, 255);
SDL_RenderClear(renderer);

// Render the combined scene with one SDL_RenderCopy call
SDL_RenderCopy(renderer, sceneTexture, NULL, NULL);

// Present everything on the screen
SDL_RenderPresent(renderer);
