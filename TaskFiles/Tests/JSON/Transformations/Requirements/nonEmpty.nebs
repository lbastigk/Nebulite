# Test assertion that a value is not empty
set existingValue value

# Scenario 1: must fail
assign global:result = {global:missingValue|require nonEmpty|unreachable}
eval nop {global:result|assert empty}

# Scenario 2: success
assign global:result = {global:existingValue|require nonEmpty}
eval nop {global:result|assert type basicValue}