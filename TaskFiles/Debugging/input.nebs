# Debugging inputs from player
set-res 200 200 1
set-fps 1
always eval echo "Value of key w: " + {global.input.keyboard.current.w}
always eval echo "Delta of key w: " + {global.input.keyboard.delta.w}