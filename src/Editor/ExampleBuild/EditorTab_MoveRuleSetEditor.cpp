#include "EditorTab_MoveRuleSetEditor.h"

MoveRuleSetEditor::MoveRuleSetEditor(QWidget *parent)
    : QWidget(parent)
{
    mainLayout = new QHBoxLayout(this);
    setLayout(mainLayout);

    // TODO: Add widgets specific to the Move Rule Set Editor
}
