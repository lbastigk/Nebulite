#include "EditorTab_RenderObjectEditor.h"

RenderObjectEditor::RenderObjectEditor(QWidget *parent)
    : QWidget(parent)
{
    mainLayout = new QHBoxLayout(this);
    setLayout(mainLayout);

    // TODO: Add widgets specific to the Render Object Editor
}
